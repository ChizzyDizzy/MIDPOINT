# ============================================================
# SafeMind AI - Hugging Face Only Configuration
# ============================================================

# Choose your AI backend:
# - huggingface: Hugging Face Inference API (FREE, cloud-based) ⭐ RECOMMENDED
# - local: Run models locally (FREE, offline, needs GPU/CPU)
# - fallback: Template-based responses (no AI)

AI_BACKEND=huggingface

# ============================================================
# Hugging Face Configuration
# ============================================================
# Get FREE API key at: https://huggingface.co/settings/tokens
# No credit card required!

HUGGINGFACE_API_KEY=hf_your_actual_key_here
HUGGINGFACE_MODEL=microsoft/DialoGPT-medium

# Alternative FREE models you can try:
# HUGGINGFACE_MODEL=facebook/blenderbot-400M-distill  (conversational)
# HUGGINGFACE_MODEL=google/flan-t5-base  (instruction following)
# HUGGINGFACE_MODEL=EleutherAI/gpt-neo-125M  (small GPT-style)
# HUGGINGFACE_MODEL=distilgpt2  (lightweight and fast)

# ============================================================
# Local Models (Completely FREE, runs offline)
# ============================================================
# No API key needed! Runs on your computer.
# Requires: pip install transformers torch
# First run downloads ~500MB, then works offline

# To use local models, set AI_BACKEND=local and uncomment below:
# LOCAL_MODEL=microsoft/DialoGPT-small  (150MB, faster)
# LOCAL_MODEL=microsoft/DialoGPT-medium  (500MB, better quality)
# LOCAL_MODEL=./your-trained-model-path  (your fine-tuned model)

# ============================================================
# Flask Configuration
# ============================================================
FLASK_SECRET_KEY=your_secret_key_here_change_in_production
FLASK_ENV=development

# ============================================================
# Feature Flags
# ============================================================
ENABLE_AI_RESPONSES=True
ENABLE_CULTURAL_ADAPTATION=True
ENABLE_CRISIS_DETECTION=True

# ============================================================
# IMPORTANT NOTES:
# ============================================================
# 1. DO NOT commit this file with real API keys to GitHub
# 2. Copy this file to .env and add your actual keys
# 3. Hugging Face API is completely FREE (recommended)
# 4. Local models are FREE but need disk space (~500MB-2GB)
# 5. First API call to HF may take 20-30 seconds (model loading)
